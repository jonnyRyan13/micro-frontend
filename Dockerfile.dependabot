# Use an official Ruby image as the base image.
# You can adjust the Ruby version as needed.
FROM ruby:2.7

# Set the working directory.
WORKDIR /code

# Install Dependabot and other dependencies.
RUN gem install dependabot --no-document

# Specify the entry point for running Dependabot.
CMD ["dependabot"]